1-Poder escribir en el CSV de los comandos de la terminal.
2-Poder "instalar" paquetes (se escrbiran nuevas celdas en el CSV).

--- formato de instalacion ---
csv_name
{
	dictionary /// command
	{
		dictionary[] /// list of possible commands
		response[] /// list of responses
	}
	class or directory with classes /// buscar si se pueden inyectar clases en un juego ya empaquetado (lo dudo)
	{
	}
}

3-Poder crear entornos virtuales (sera crear nuevos CSV en blanco).
4-Optimizar las busquedas dentro del CSV (usar un arbol).
5-Preparar la terminal para lidiar con archivos cuyo directorio tiene denegada la inspeccion.